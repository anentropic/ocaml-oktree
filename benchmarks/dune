(executable
 (name nearest)
 (modules nearest)
 (libraries core_bench core_unix gg oktree)
 (enabled_if
  (= %{profile} test)))

(env
 (dev
  (flags
   (:standard -warn-error -A)))
 (test
  (flags
   (:standard -warn-error -A))))
